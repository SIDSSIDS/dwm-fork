#!/bin/bash

code=$(xset -q | grep LED | egrep -o 'LED mask: +[0-9]+' | egrep -o '[0-9]+')

if [[ "${code:4:1}" == "0" ]]; then
    echo -n "EN"
elif [[ "${code:4:1}" == "1" ]]; then
    echo -n "RU"
else
    echo -n "??"
fi
