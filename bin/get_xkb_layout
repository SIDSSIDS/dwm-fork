#!/bin/bash

code=$(xset -q | grep LED | grep -E -o 'LED mask: +[0-9]+' | grep -E -o '[0-9]+')

layout_code="${code:4:1}"
capslock_on=$(( ${code:7:1} % 2 ))
numlock_on=$((  ${code:7:1} / 2 ))

echo -n "$layout_code$capslock_on$numlock_on"
